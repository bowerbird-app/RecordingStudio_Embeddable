<% page = @page || @parent_recordable %>
<article style="font-family:system-ui,sans-serif;padding:1rem">
  <h1><%= page&.title || "Page" %></h1>
  <p><%= page&.description.presence || "No description provided." %></p>
</article>
