===============================================================================

RecordingStudioEmbeddable has been installed successfully.

Next steps:
1. Run `bin/rails db:migrate`.
2. Opt recordable classes in with `recording_studio_embeddable renderer: "..."`.
3. Enable an embed with `recording.ensure_embed!` and visit `/recording_studio_embeddable/embeds/:token`.

===============================================================================
